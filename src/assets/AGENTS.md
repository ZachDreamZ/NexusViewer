# src/assets

## Purpose
Store static assets and images for the NexusViewer app.

## Ownership
Agent

## Local Contracts
- Use optimized formats (SVG, PNG).
- Maintain consistent naming conventions for assets.

## Work Guidance
Add assets to this folder and import them directly into components or CSS.

## File-specific contracts

### logo.svg
- Generated by `scripts/generate_logo.py` (do not hand-edit; the script is the source of truth).
- Three connected nodes forming a triangle with a central ring + dot, brand cyan `#00f2ff` on a transparent background.
- 64×64 viewBox, scales cleanly to the 24×24 header mark and 72×72 welcome mark.
- Imported into `Layout.tsx` (header) and `Welcome.tsx` (landing page).

## Verification
Ensure assets are correctly linked and render without 404 errors.

## Child DOX Index
None
